import React from 'react';

export default class TableWithData extends Component {
  constructor(props) {
    super(props);
    this.state = {
    };
  }

  render() {
    return (
      <div></div>
    );
  }
}
